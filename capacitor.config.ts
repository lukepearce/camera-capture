import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'camera-capture',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
